export const DEFAULT_DEPLOY_GAS = 10000000;
export const DEFAULT_REGISTER_GAS = 200000;
export const DEFAULT_MINT_GAS = 200000;
export const DEFAULT_GAS = 100000;

const Constants = {
    DEFAULT_DEPLOY_GAS,
    DEFAULT_GAS,
    DEFAULT_MINT_GAS,
    DEFAULT_REGISTER_GAS,
};

export default Constants;